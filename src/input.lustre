node Counter(init1: int,init2: int) returns (count1: int,count2: int, putasd : bool);
var
    x: int;
    y: bool;
let
  count1 = init1 -> pre count1 + 1;
  count2 = init2 -> pre count2 + 1;
  putasd = 1 = 3;
tel;
