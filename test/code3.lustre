node Counter(init1: int;init2: int) returns (count1: int;count2: int);
var
    x: int;
    y: int;
    t: int;
let
  init1 = x + 1;
  init2 = y + 1;
  count1 = init1 -> count1 + 1;
  count1 = count1 + x;
  count2 = t + 2;
tel;
